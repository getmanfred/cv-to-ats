// This component is no longer used — suggestions are rendered via SuggestionCard.
// Kept to avoid breaking imports if any external reference exists.
export default function SuggestionList() {
  return null
}
